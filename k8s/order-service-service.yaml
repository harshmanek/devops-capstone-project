apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: microservices
spec:
  selector:
    app: order-service
  ports:
    - name: http-metrics
      protocol: TCP
      port: 5003
      targetPort: 5003
  type: ClusterIP
